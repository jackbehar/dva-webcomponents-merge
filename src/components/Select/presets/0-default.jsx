import * as React from "react";
import Select from "../Select";

export default (
  <Select uxpId="select-1" label="Select Label">
    <option value={`1`}>One</option>
    <option value={`2`}>Two</option>
  </Select>
);
